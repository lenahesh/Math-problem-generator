<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>הרפתקה מתמטית - משחק לילדים</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Assistant:wght@400;700&family=Varela+Round&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #FF6B6B;
            --secondary: #4ECDC4;
            --accent: #FFE66D;
        }
        body {
            font-family: 'Varela Round', 'Assistant', sans-serif;
            background: #87CEEB;
            min-height: 100vh;
            margin: 0;
            overflow-x: hidden;
            position: relative;
        }
        .cloud {
            position: absolute;
            background: white;
            border-radius: 50px;
            opacity: 0.8;
            z-index: -1;
            animation: move linear infinite;
        }
        @keyframes move {
            from { right: -200px; }
            to { right: 100vw; }
        }
        .game-card {
            background: rgba(255, 255, 255, 0.95);
            border: 8px solid var(--secondary);
            border-radius: 3rem;
            box-shadow: 0 20px 0 rgba(0,0,0,0.1);
        }
        .btn-game {
            background: var(--primary);
            color: white;
            border-bottom: 6px solid #d64545;
            transition: all 0.1s;
        }
        .btn-game:active {
            border-bottom-width: 0px;
            transform: translateY(6px);
        }
        .option-card {
            cursor: pointer;
            transition: all 0.2s ease;
            border: 4px solid #eee;
            border-radius: 1.5rem;
        }
        .option-card.active {
            border-color: var(--secondary);
            background-color: #e0fffb;
            transform: scale(1.05);
        }
        input[type="number"] {
            direction: ltr;
            text-align: center;
        }
        .character-bounce {
            animation: bounce 2s infinite ease-in-out;
        }
        @keyframes bounce {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(5deg); }
        }
    </style>
</head>
<body class="p-4 md:p-8">

    <!-- Background Clouds -->
    <div class="cloud w-32 h-12" style="top: 10%; animation-duration: 25s;"></div>
    <div class="cloud w-48 h-16" style="top: 25%; animation-duration: 35s; animation-delay: -5s;"></div>
    <div class="cloud w-40 h-14" style="top: 60%; animation-duration: 30s; animation-delay: -15s;"></div>

    <nav class="max-w-4xl mx-auto mb-6 flex justify-between items-center bg-white/90 p-4 rounded-3xl shadow-lg border-b-4 border-gray-200">
        <div class="flex items-center gap-3">
            <span class="text-4xl character-bounce">🦊</span>
            <h1 class="text-2xl font-bold text-orange-500 text-center">הרפתקה מתמטית</h1>
        </div>
        <div class="flex gap-3 items-center">
            <button onclick="location.reload()" class="bg-blue-500 text-white px-6 py-2 rounded-full font-bold shadow-md hover:bg-blue-600">בית 🏠</button>
        </div>
    </nav>

    <main class="max-w-4xl mx-auto relative">
        <div id="setupScreen" class="game-card p-6 md:p-10">
            <div class="text-center mb-8">
                <h2 class="text-3xl font-bold text-blue-600 mb-2">איזה כיף שבאתם!</h2>
                <p class="text-gray-600 text-lg">בואו נבנה לכם אתגר מיוחד:</p>
            </div>
            
            <div class="space-y-8">
                <div>
                    <label class="block text-xl font-bold mb-4 text-gray-700">🌈 בחר נושא לסיפורים:</label>
                    <div class="grid grid-cols-3 md:grid-cols-5 gap-3" id="themeContainer"></div>
                </div>

                <div class="grid md:grid-cols-2 gap-8">
                    <div>
                        <label class="block text-xl font-bold mb-4 text-gray-700">⚡ רמת מורכבות הבעיה:</label>
                        <select id="complexity" class="w-full p-4 rounded-2xl border-4 border-gray-100 focus:border-cyan-400 outline-none text-lg font-bold">
                            <option value="1">פשוטה (שלב אחד)</option>
                            <option value="2">מאתגרת (שני שלבים)</option>
                            <option value="3">אלוף (רב-שלבית)</option>
                        </select>
                    </div>
                    <div>
                        <label class="block text-xl font-bold mb-4 text-gray-700">🔢 טווח מספרים:</label>
                        <select id="range" class="w-full p-4 rounded-2xl border-4 border-gray-100 focus:border-cyan-400 outline-none text-lg font-bold">
                            <option value="20">עד 20</option>
                            <option value="100">עד 100</option>
                            <option value="1000">עד 1,000</option>
                            <option value="10000">עד 10,000</option>
                        </select>
                    </div>
                </div>

                <div>
                    <label class="block text-xl font-bold mb-4 text-gray-700">🧠 מה נתרגל היום?</label>
                    <div class="grid grid-cols-2 md:grid-cols-3 gap-3" id="subjectContainer"></div>
                </div>

                <button id="startGameBtn" class="w-full py-6 btn-game text-white text-3xl font-bold rounded-3xl shadow-xl mt-4">
                    יאללה, יוצאים לדרך! 🚀
                </button>
            </div>
        </div>

        <div id="gameScreen" class="game-card p-6 md:p-10 hidden text-center">
            <div class="mb-6 flex justify-between items-center">
                <div class="bg-yellow-100 border-2 border-yellow-400 text-yellow-700 px-6 py-2 rounded-full font-bold text-lg" id="statusLabel">שאלה 1</div>
                <div class="bg-green-100 border-2 border-green-400 text-green-700 px-6 py-2 rounded-full font-bold text-lg" id="scoreLabel">כוכבים: 0 ⭐</div>
            </div>

            <div class="relative bg-white border-4 border-dashed border-blue-200 p-8 rounded-3xl mb-8 min-h-[220px] flex items-center justify-center">
                <p id="problemText" class="text-2xl md:text-3xl leading-relaxed text-gray-800 font-bold"></p>
            </div>

            <div class="flex flex-col items-center gap-6">
                <div class="relative w-full max-w-xs">
                    <input type="number" id="userAnswer" placeholder="רשמו תשובה..." class="w-full p-6 text-4xl border-4 border-cyan-200 rounded-3xl focus:border-cyan-500 outline-none shadow-inner">
                </div>
                
                <div class="flex gap-4 w-full max-w-md">
                    <button id="hintBtn" class="flex-1 py-4 bg-orange-400 text-white text-xl font-bold rounded-2xl hover:bg-orange-500 shadow-md">
                        צריך רמז? 💡
                    </button>
                    <button id="checkAnswerBtn" class="flex-[2] py-4 btn-game text-white text-2xl font-bold rounded-2xl">
                        בדוק תשובה ✅
                    </button>
                    <button id="skipBtn" class="flex-1 py-4 bg-gray-400 text-white text-xl font-bold rounded-2xl hover:bg-gray-500 shadow-md">
                        דלג ⏭️
                    </button>
                </div>
            </div>
            <div id="hintBox" class="mt-4 p-4 rounded-2xl bg-orange-50 text-orange-800 border-2 border-orange-200 hidden italic text-lg"></div>
            <div id="feedback" class="mt-8 p-4 rounded-2xl text-2xl font-bold hidden transform transition-all"></div>
        </div>
    </main>

    <script>
        const THEMES = [
            { id: 'gaming', name: 'גיימינג', icon: '🎮' },
            { id: 'soccer', name: 'כדורגל', icon: '⚽' },
            { id: 'animals', name: 'חיות', icon: '🦁' },
            { id: 'science', name: 'מדע', icon: '🧪' },
            { id: 'basketball', name: 'כדורסל', icon: '🏀' },
            { id: 'cars', name: 'מכוניות', icon: '🚗' },
            { id: 'cooking', name: 'בישול', icon: '🧁' },
            { id: 'shopping', name: 'קניות', icon: '🛒' },
            { id: 'space', name: 'חלל', icon: '🚀' }
        ];

        const SUBJECTS = [
            { id: 'addsub', name: 'חיבור וחיסור' },
            { id: 'muldiv', name: 'כפל וחילוק' },
            { id: 'mixed', name: 'הכל מהכל' },
            { id: 'comparison', name: 'השוואה כפלית' }
        ];

        const CHARACTERS = [
            { name: "דני", gender: "m" }, { name: "יניב", gender: "m" },
            { name: "איתי", gender: "m" }, { name: "רון", gender: "m" },
            { name: "נועה", gender: "f" }, { name: "מאיה", gender: "f" },
            { name: "שירה", gender: "f" }, { name: "עדי", gender: "f" }
        ];

        let gameState = {
            theme: 'gaming',
            complexity: 1,
            range: 100,
            subject: 'addsub',
            score: 0,
            currentProblem: null,
            usedProblems: [],
            questionCount: 1
        };

        const setupScreen = document.getElementById('setupScreen');
        const gameScreen = document.getElementById('gameScreen');
        const themeContainer = document.getElementById('themeContainer');
        const subjectContainer = document.getElementById('subjectContainer');
        const problemText = document.getElementById('problemText');
        const userAnswer = document.getElementById('userAnswer');
        const feedback = document.getElementById('feedback');
        const hintBox = document.getElementById('hintBox');
        const scoreLabel = document.getElementById('scoreLabel');
        const statusLabel = document.getElementById('statusLabel');

        function getVerb(base, gender) {
            const verbs = {
                "זכה": { m: "זכה", f: "זכתה" },
                "הבקיע": { m: "הבקיע", f: "הבקיעה" },
                "קנה": { m: "קנה", f: "קנתה" },
                "מצא": { m: "מצא", f: "מצאה" },
                "אסף": { m: "אסף", f: "אספה" },
                "הלך": { m: "הלך", f: "הלכה" },
                "רצה": { m: "רצה", f: "רצתה" },
                "ראה": { m: "ראה", f: "ראתה" },
                "הביא": { m: "הביא", f: "הביאה" },
                "גילה": { m: "גילה", f: "גילתה" },
                "קלע": { m: "קלע", f: "קלעה" },
                "הכין": { m: "הכין", f: "הכינה" },
                "נהג": { m: "נהג", f: "נהגה" }
            };
            return verbs[base] ? verbs[base][gender] : base;
        }

        function generateProblem() {
            const r = parseInt(gameState.range);
            const comp = parseInt(gameState.complexity);
            const sub = gameState.subject;
            const rand = (min, max) => Math.floor(Math.random() * (max - min + 1)) + min;

            let q = "", a = 0, hint = "";
            
            let charIdx1 = rand(0, CHARACTERS.length - 1);
            let charIdx2 = (charIdx1 + rand(1, CHARACTERS.length - 1)) % CHARACTERS.length;
            const p1 = CHARACTERS[charIdx1];
            const p2 = CHARACTERS[charIdx2];

            if (sub === 'comparison') {
                const factor = rand(2, 5);
                if (gameState.theme === 'basketball') {
                    const n1 = rand(2, 8);
                    q = `${p1.name} ${getVerb("קלע", p1.gender)} ${n1} שלשות במשחק. ${p2.name} ${getVerb("קלע", p2.gender)} פי ${factor} יותר שלשות מ${p1.name}. כמה שלשות ${getVerb("קלע", p2.gender)} ${p2.name}?`;
                    a = n1 * factor;
                    hint = `נסו לחשוב: אם ל${p1.name} יש ${n1}, וצריך פי ${factor}, איזו פעולה חשבונית עושים?`;
                } else if (gameState.theme === 'shopping') {
                    const price = rand(10, r/factor);
                    q = `${p1.name} ${getVerb("קנה", p1.gender)} חולצה ב-${price} שקלים. ${p2.name} ${getVerb("קנה", p2.gender)} נעליים שמחירן פי ${factor} מהחולצה. כמה עלו הנעליים של ${p2.name}?`;
                    a = price * factor;
                    hint = `המחיר של הנעליים גדול פי ${factor} מ-${price}.`;
                } else {
                    const n1 = rand(5, Math.floor(r/factor));
                    q = `ל${p1.name} יש ${n1} פריטים. ל${p2.name} יש פי ${factor} יותר. כמה יש ל${p2.name}?`;
                    a = n1 * factor;
                    hint = `פעולת כפל תעזור כאן!`;
                }
            } 
            else if (sub === 'addsub' || sub === 'mixed') {
                if (gameState.theme === 'basketball') {
                    const n1 = rand(15, r/2);
                    const n2 = rand(5, r/4);
                    q = `במשחק הדרבי בין מכבי תל אביב להפועל ירושלים, ${p1.name} ${getVerb("קלע", p1.gender)} ${n1} נקודות במחצית הראשונה. בסוף המשחק התברר ש${p1.gender === 'm' ? 'הוא' : 'היא'} ${getVerb("קלע", p1.gender)} בסך הכל ${n1+n2} נקודות. כמה נקודות ${p1.name} ${getVerb("קלע", p1.gender)} במחצית השנייה?`;
                    a = n2;
                    hint = `נחסיר את הנקודות של המחצית הראשונה מהסך הכל כדי למצוא את ההפרש.`;
                } else if (gameState.theme === 'shopping') {
                    const budget = rand(100, r);
                    const item1 = rand(20, budget/2);
                    const item2 = rand(10, budget/4);
                    q = `ל${p1.name} היו ${budget} שקלים בארנק. ${p1.gender === 'm' ? 'הוא' : 'היא'} ${getVerb("קנה", p1.gender)} כדור ב-${item1} שקלים ומשחק קופסה ב-${item2} שקלים. כמה עודף נשאר ל${p1.name}?`;
                    a = budget - item1 - item2;
                    hint = `קודם חשבו כמה כסף ${p1.name} הוציא בסך הכל, ואז החסירו מהסכום ההתחלתי.`;
                } else if (gameState.theme === 'animals') {
                    const n1 = rand(10, r/2);
                    const n2 = rand(5, r/3);
                    q = `בחוות החיות של ${p1.name} היו ${n1} כבשים. בלילה נולדו עוד ${n2} טלאים קטנים. כמה חיות יש עכשיו בחוות הכבשים?`;
                    a = n1 + n2;
                    hint = `זו בעיית חיבור פשוטה. כמה היו ועוד כמה נוספו?`;
                } else {
                    const n1 = rand(10, r/2), n2 = rand(1, r/2);
                    q = `ל${p1.name} היו ${n1} פריטים ו${p2.name} ${getVerb("הביא", p2.gender)} עוד ${n2}. כמה יש עכשיו?`;
                    a = n1 + n2;
                    hint = `חברו את שני המספרים.`;
                }
            }
            else if (sub === 'muldiv') {
                const n1 = rand(3, 10), n2 = rand(2, 9);
                if (gameState.theme === 'cooking') {
                    q = `${p1.name} ${getVerb("הכין", p1.gender)} ${n1} תבניות של עוגיות. בכל תבנית יש בדיוק ${n2} עוגיות שוקולד צ'יפס. כמה עוגיות ${p1.name} ${getVerb("הכין", p1.gender)} בסך הכל?`;
                    a = n1 * n2;
                    hint = `דמיינו ${n1} שורות של עוגיות, כשבכל שורה יש ${n2}.`;
                } else if (gameState.theme === 'soccer') {
                    const total = n1 * n2;
                    q = `מאמן הכדורגל ${p1.name} הביא לאימון ${total} כדורים. הוא ביקש מ${p2.name} לחלק אותם שווה בשווה בין ${n1} קבוצות. כמה כדורים קיבלה כל קבוצה?`;
                    a = n2;
                    hint = `כאן צריך לבצע פעולת חילוק: ${total} לחלק ל-${n1}.`;
                } else {
                    q = `ל${p1.name} יש ${n1} שקיות, ובכל שקית ${n2} סוכריות. כמה יש בסך הכל?`;
                    a = n1 * n2;
                    hint = `כפל יעזור לכם למצוא את הסך הכל מהר!`;
                }
            }

            // High Complexity: Multistep
            if (comp >= 2 && sub !== 'comparison') {
                const extra = rand(5, 15);
                if (Math.random() > 0.5) {
                    q += ` לאחר מכן, הגיע ${CHARACTERS[rand(0,7)].name} ולקח ${extra} מהם. כמה נשארו?`;
                    a -= extra;
                } else {
                    q += ` ואז ${CHARACTERS[rand(0,7)].name} הוסיף עוד ${extra} משלו. מה הסכום עכשיו?`;
                    a += extra;
                }
                hint = "שימו לב, יש כאן שני שלבים! קודם פתרו את החלק הראשון ואז את השני.";
            }

            if (gameState.usedProblems.includes(q)) return generateProblem();
            gameState.usedProblems.push(q);
            return { q, a, hint };
        }

        function initUI() {
            themeContainer.innerHTML = '';
            THEMES.forEach(t => {
                const div = document.createElement('div');
                div.className = `option-card p-4 bg-white flex flex-col items-center gap-1 ${gameState.theme === t.id ? 'active' : ''}`;
                div.innerHTML = `<span class="text-3xl">${t.icon}</span><span class="text-sm font-bold">${t.name}</span>`;
                div.onclick = () => {
                    document.querySelectorAll('#themeContainer .option-card').forEach(el => el.classList.remove('active'));
                    div.classList.add('active');
                    gameState.theme = t.id;
                };
                themeContainer.appendChild(div);
            });

            subjectContainer.innerHTML = '';
            SUBJECTS.forEach(s => {
                const div = document.createElement('div');
                div.className = `option-card p-3 bg-white text-center font-bold ${gameState.subject === s.id ? 'active' : ''}`;
                div.innerText = s.name;
                div.onclick = () => {
                    document.querySelectorAll('#subjectContainer .option-card').forEach(el => el.classList.remove('active'));
                    div.classList.add('active');
                    gameState.subject = s.id;
                };
                subjectContainer.appendChild(div);
            });
        }

        document.getElementById('startGameBtn').onclick = () => {
            gameState.complexity = parseInt(document.getElementById('complexity').value);
            gameState.range = parseInt(document.getElementById('range').value);
            gameState.score = 0;
            gameState.questionCount = 1;
            setupScreen.classList.add('hidden');
            gameScreen.classList.remove('hidden');
            nextQuestion();
        };

        function nextQuestion() {
            gameState.currentProblem = generateProblem();
            problemText.innerText = gameState.currentProblem.q;
            userAnswer.value = '';
            feedback.classList.add('hidden');
            hintBox.classList.add('hidden');
            statusLabel.innerText = `שאלה ${gameState.questionCount}`;
            scoreLabel.innerText = `כוכבים: ${gameState.score} ⭐`;
            userAnswer.focus();
        }

        document.getElementById('hintBtn').onclick = () => {
            hintBox.innerText = gameState.currentProblem.hint;
            hintBox.classList.remove('hidden');
        };

        document.getElementById('skipBtn').onclick = () => {
            gameState.questionCount++;
            nextQuestion();
        };

        document.getElementById('checkAnswerBtn').onclick = checkAnswer;
        userAnswer.onkeydown = (e) => { if(e.key === 'Enter') checkAnswer(); };

        function checkAnswer() {
            const val = parseInt(userAnswer.value);
            if (isNaN(val)) return;
            feedback.classList.remove('hidden');
            if (val === gameState.currentProblem.a) {
                gameState.score += 10;
                gameState.questionCount++;
                feedback.innerHTML = "כל הכבוד! פתרתם נכון! 🌟🏆";
                feedback.className = "mt-8 p-4 rounded-2xl text-2xl font-bold bg-green-100 text-green-700 animate-bounce";
                setTimeout(nextQuestion, 2000);
            } else {
                feedback.innerHTML = "לא בדיוק... נסו להיעזר ברמז! 💪";
                feedback.className = "mt-8 p-4 rounded-2xl text-2xl font-bold bg-red-100 text-red-600";
            }
        }
        initUI();
    </script>
</body>
</html>